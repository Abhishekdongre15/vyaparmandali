import 'package:flutter/material.dart';
class ItemHamaliExpView extends StatelessWidget {
  const ItemHamaliExpView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Item Hamali exp"),
    );
  }
}
