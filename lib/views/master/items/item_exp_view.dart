import 'package:flutter/material.dart';
class ItemExpView extends StatelessWidget {
  const ItemExpView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Item exp"),
    );
  }
}
